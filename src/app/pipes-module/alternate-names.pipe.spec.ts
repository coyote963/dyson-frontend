import { AlternateNamesPipe } from './alternate-names.pipe';

describe('AlternateNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new AlternateNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
